import React, {FunctionComponent} from "react";
import ReactLoading from 'react-loading';

const Loading: FunctionComponent = () =>
    <ReactLoading type='spinningBubbles' color="#016066" height={'10%'} width={'10%'} />

export default Loading;
